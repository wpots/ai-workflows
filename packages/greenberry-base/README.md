# Greenberry Base

Base preset for Greenberry projects.

Use this package for shared defaults that should apply broadly across
Greenberry-maintained repositories, independent of framework or product.

Expected contents over time:

- baseline shared conventions
- shared repo structure guidance
- default sync or adoption metadata
- references to core `rules/`, `skills/`, and `commands/`
